steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - -f
      - services/sync/Dockerfile
      - .
images:
  - ${_IMAGE}
substitutions:
  _IMAGE: sync:latest
options:
  logging: CLOUD_LOGGING_ONLY
