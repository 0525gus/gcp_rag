# 상한을 반드시 둘 것 
starlette>=0.40.0,<0.47.0
# 상한 필수 — 2.0 이 `mcp.server.fastmcp` 를 없앴다. services/mcp_server/main.py 가
# 그 경로에서 FastMCP 를 임포트하므로, 상한이 없으면 다음 이미지 빌드가 2.x 를 받아
# 컨테이너가 기동 즉시 ModuleNotFoundError 로 죽는다(:latest 태그라 무증상 배포됨).
# 2.x 로 올리려면 FastMCP 임포트 경로부터 옮기고 검색 경로를 재검증할 것.
mcp>=1.1.0,<2
uvicorn[standard]>=0.32.0,<1
pydantic>=2.9.0,<3
google-cloud-firestore>=2.19.0,<3
# 하한 필수 — shared/rag_engine.py 가 `from agentplatform import rag` 를 쓴다.
# agentplatform 패키지는 이 배포판 안에 들어있고 1.149.0 부터 존재한다.
# (폐기 예고된 vertexai.rag 에서 이관 완료. 되돌리지 말 것.)
# 상한 필수 — 2.x 는 API 변경 폭이 커서 재검증 전엔 올리지 않는다.
google-cloud-aiplatform>=1.149.0,<2
google-auth>=2.35.0,<3
