비었다. docs/ENV_MIGRATION.md 3~6 단계 + 실배포까지 끝났다 (2026-08-23).

운영상 남은 판단거리 하나:
  MCP 키가 추측 가능한 값이다. ALLOW_UNAUTH=true 라 엔드포인트가 공개이고
  경계는 이 키뿐이라, 유출 없이 추측만으로 코퍼스 전량이 열린다.
  바꾸려면: config/departments/<학과>.yaml 의 keys 를 고치고
    .\scripts\deploy_mcp.ps1 -All -SkipBuild
  그다음 커넥터 키 갱신 + 골든 스냅샷 재생성
  (tests/test_deploy_env_parity.py 의 길이 대조가 안 맞으면 잡아준다).
